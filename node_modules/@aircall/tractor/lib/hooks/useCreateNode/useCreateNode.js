"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useCreateNode = void 0;

var _react = require("react");

var useCreateNode = function useCreateNode(container, hideNode) {
  if (container === void 0) {
    container = document.body;
  }

  var defaultNode = (0, _react.useRef)(null);
  (0, _react.useEffect)(function () {
    // Unmount the created node.
    if (hideNode) {
      return;
    }

    var node = document.createElement('div');
    node.setAttribute('data-test', 'node');
    defaultNode.current = node;
    container.appendChild(defaultNode.current);
    return function () {
      if (defaultNode.current) {
        container.removeChild(defaultNode.current);
        defaultNode.current = null;
      }
    };
  }, [container, hideNode]);
  return defaultNode.current;
};

exports.useCreateNode = useCreateNode;
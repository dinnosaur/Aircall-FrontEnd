import { BoxProps } from '../Box';
import { Toast, InternalToast } from './ToastItem.decl';
export interface ToastCtx {
    /**
     * List of "visible" toasts.
     */
    toasts: InternalToast[];
    /**
     * Function which lets you display a toast item on the page.
     * This function also returns a cleanup function which gives you the ability
     * to remove the toast "manually".
     */
    showToast: (toast: Toast) => () => void;
    /**
     * Function which lets you remove a given toast by its ID from the list.
     */
    removeToast: (id: string) => void;
}
export interface ToastConfigCtx {
    position: BoxProps['position'];
    takeContainerWidth: boolean;
}

import { BoxProps } from '../Box';
export interface ToastManagerProps {
    /**
     * Container on which the Toasts List would be render in.
     * Defaults to document.body.
     */
    container?: HTMLElement | (() => HTMLElement);
    /**
     * Whether or not the toast items need to take the full width of the container.
     * Defaults to false.
     */
    takeContainerWidth?: boolean;
    /**
     * Position of the Toasts List Container.
     * Defaults to fixed.
     */
    position?: BoxProps['position'];
}

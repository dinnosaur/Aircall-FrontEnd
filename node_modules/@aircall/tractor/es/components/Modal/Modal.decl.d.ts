/// <reference types="react" />
export declare type ModalSizes = 'xSmall' | 'small' | 'regular' | 'large';
export interface ModalProps {
    children: React.ReactNode;
    /** When `true`, the modal will show itself */
    show: boolean;
    /** When `true`, `onHide` will be triggered if backdrop is clicked */
    rootClose?: boolean;
    /** A callback fired when the header closeButton or backdrop is clicked */
    onHide: () => void;
    /**	The size of modal */
    size?: ModalSizes;
    /** The root element where the modal will be appended */
    portalContainer?: HTMLElement;
}
export interface ModalHeaderProps {
    closeButton?: boolean;
    children: React.ReactNode;
}

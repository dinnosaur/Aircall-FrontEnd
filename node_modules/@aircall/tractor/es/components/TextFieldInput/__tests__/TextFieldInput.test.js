import React from 'react';
import userEvent from '@testing-library/user-event';
import { TextFieldInput } from '..';
import { render, screen } from '../../../tests/test-utils';
describe('TextFieldInput', function () {
  describe('clearable', function () {
    it('should not clear the text by clicking enter key', function () {
      var onSubmitMock = jest.fn();
      render( /*#__PURE__*/React.createElement("form", {
        onSubmit: onSubmitMock
      }, /*#__PURE__*/React.createElement(TextFieldInput, {
        size: "small",
        onClear: jest.fn(),
        "data-test": "input-field"
      })));
      var input = screen.getByTestId('input-field'); // type something in input field

      userEvent.type(input, 'something'); // type "enter" key in input field

      userEvent.type(input, '{enter}'); // should not clear value

      expect(input).toHaveValue('something');
      expect(onSubmitMock).toHaveBeenCalled();
    });
  });
});
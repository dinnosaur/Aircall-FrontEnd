import { ReactNode } from 'react';
import { LayoutProps } from 'styled-system';
import { ButtonProps } from '../Button';
export interface SquareButtonProps extends Omit<ButtonProps, 'variant' | 'size' | 'mode' | 'direction' | 'block'> {
    /**
     * Defines the color or the variant of the SquareButton.
     *
     * Possible values: `primary` | `destructive` | `instructive` | `warning` | `ghost` | `darkGhost` | `default`
     */
    variant?: SquareButtonVariants;
    /**
     * Defines the size of the square.
     */
    square?: LayoutProps['size'];
    children?: ReactNode;
}
export interface SquareButtonContainerProps extends Omit<ButtonProps, 'variant' | 'size' | 'mode' | 'block'> {
    variant: SquareButtonVariants;
    /**
     * Defines the size of the square.
     */
    square?: LayoutProps['size'];
}
export declare enum SquareButtonVariant {
    PRIMARY = "primary",
    DESTRUCTIVE = "destructive",
    INSTRUCTIVE = "instructive",
    WARNING = "warning",
    GHOST = "ghost",
    DARK_GHOST = "darkGhost",
    BLACK = "black",
    WHITE = "white",
    DEFAULT = "default"
}
export declare type SquareButtonVariants = 'primary' | 'destructive' | 'instructive' | 'warning' | 'ghost' | 'darkGhost' | 'black' | 'white' | 'default';

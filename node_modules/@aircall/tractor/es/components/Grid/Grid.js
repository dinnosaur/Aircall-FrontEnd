import styled from 'styled-components';
import { flexbox } from 'styled-system';
import { baseTheme } from '../../styles';
import { Box } from '../Box';
import { grid, shouldForwardProp } from './utils';
/**
 * A grid which supports arbitrary margins and paddings and grid properties, where necessary.
 * 🚨Use scarcely!
 */

var Grid = styled(Box).withConfig({
  shouldForwardProp: shouldForwardProp
}).withConfig({
  displayName: "Grid",
  componentId: "sc-179hakm-0"
})(["", " ", ""], grid, flexbox);
Grid.defaultProps = {
  theme: baseTheme,
  display: 'grid'
};
export default Grid;
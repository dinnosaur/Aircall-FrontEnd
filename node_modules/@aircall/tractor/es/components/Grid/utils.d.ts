import { StyledConfig } from 'styled-components';
export declare const shouldForwardProp: StyledConfig['shouldForwardProp'];
/**
 * Only define the parent grid attributes because the other grid items are already defined on the Box component.
 */
export declare const grid: import("styled-system").styleFn;

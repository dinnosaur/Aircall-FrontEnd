import { BoxProps } from './../Box/Box.decl';
declare type SkeletonShape = 'square' | 'squareRounded' | 'circle' | 'squircle';
declare type SkeletonAnimation = 'wave' | 'shimmer';
export interface SkeletonComponentProps {
    /**
     * The shape of the Skeleton, which can be either square, square with rounded corners, a circle, or a squircle.
     */
    shape?: SkeletonShape;
    /**
     * A Skeleton animates in two ways: a wave runs across it, or it shimmers, pulsating slowly.
     */
    animation?: SkeletonAnimation | null;
    /**
     * Sometimes the skeleton loading animation needs a delay.
     */
    animationDelay?: boolean;
}
export declare type SkeletonProps = BoxProps & SkeletonComponentProps;
export {};

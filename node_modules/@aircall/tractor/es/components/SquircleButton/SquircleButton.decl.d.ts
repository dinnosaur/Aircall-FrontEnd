import { ComponentType, SVGProps } from 'react';
import { ButtonProps, ButtonSizes } from '../Button';
export interface SquircleButtonProps extends Omit<ButtonProps, 'variant'> {
    /**
     * The component used as an icon.
     **/
    icon: ComponentType<SVGProps<SVGSVGElement>>;
    /**
     * Defines the color or the variant of the button.
     *
     * Possible values: `primary` | `destructive` | `instructive` | `warning` | `ghost` | `darkGhost` | `default`
     */
    variant?: SquircleButtonVariants;
    /**
     * Defines whether the icon should spin
     */
    spin?: boolean;
}
export interface SquircleButtonContainerProps extends Omit<ButtonProps, 'size' | 'variant'> {
    size: Omit<ButtonSizes, 'xSmall'>;
    variant: SquircleButtonVariants;
}
export declare enum SquircleButtonVariant {
    PRIMARY = "primary",
    DESTRUCTIVE = "destructive",
    INSTRUCTIVE = "instructive",
    WARNING = "warning",
    GHOST = "ghost",
    DARK_GHOST = "darkGhost",
    BLACK = "black",
    WHITE = "white",
    DEFAULT = "default"
}
export declare type SquircleButtonVariants = 'primary' | 'destructive' | 'instructive' | 'warning' | 'ghost' | 'darkGhost' | 'black' | 'white' | 'default';

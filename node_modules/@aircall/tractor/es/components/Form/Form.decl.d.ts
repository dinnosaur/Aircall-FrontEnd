import { HTMLProps } from 'react';
import { ComponentSizes } from '../../declarations/common.decl';
export interface FormProps extends Omit<HTMLProps<HTMLFormElement>, 'size'> {
    /**
     * Defines the size of the form components used inside of the Form.
     *
     * Possible values: `small` | `regular` | `large`
     **/
    size?: ComponentSizes;
}

import { themeGet } from '@styled-system/theme-get';
import * as styledSystem from 'styled-system';
import { baseTheme } from '../styles';
/**
 * Used to grab a value from the theme, if the theme is not provided
 * it'll default to the base tractor theme
 * @param {string} key theme key
 */

export var fromTheme = function fromTheme(key, fallback) {
  var _styledSystem$get;

  return themeGet(key, (_styledSystem$get = styledSystem.get(baseTheme, key)) != null ? _styledSystem$get : fallback);
};
import { FC } from 'react';
import { ToastManagerProps } from '../components';
import { IconContextProps } from '../components/Icon/context';
import { ThemeProviderProps } from '../styles/ThemeProvider';
export interface TractorProps extends ThemeProviderProps {
    /**
     * Either to inject global styles or not.
     *
     * By default, the styles won't be injected.
     */
    injectStyle?: boolean;
    icons?: IconContextProps['icons'];
    /**
     * Toast configuration
     */
    toastConfig?: ToastManagerProps;
}
declare const Tractor: FC<TractorProps>;
export default Tractor;
